# Development (Vite proxy / local Express)
VITE_API_URL=http://localhost:5000/api

# Production builds use client/.env.production → Render API.
# Never set this to "/api" for cPanel static hosting — Apache has no API.
